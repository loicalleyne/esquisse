# Adversarial Review — P3-reviewer-planner-hardening — Iteration 0

**Date:** 2026-04-30
**Reviewer:** Adversarial-r0 (GPT-4.1)
**Plan Slug:** P3-reviewer-planner-hardening

---

## 1. False Assumptions Hunt

- **Assumption:** All referenced files and sections exist and are writable (e.g., `.github/agents/Adversarial-r0.agent.md`, `esquisse-mcp/adversarial.go`, `skills/adversarial-review/SKILL.md`).
- **Assumption:** The `validateSlug` function is always called before using `params.PlanSlug` as a directory name (P3-008).
- **Assumption:** The Fix Type enum is not already defined in GLOSSARY.md (P3-009).
- **Assumption:** The plan assumes no concurrent edits to the same files by other tasks.

## 2. Edge Case Injection

- No explicit handling for concurrent edits to agent files or glossary (possible merge conflicts).
- No test for what happens if the nested report directory already exists but is not writable.
- No test for missing or malformed Fix Type table rows in reviewer output.

## 3. Security Adversary

- **Path traversal:** The plan relies on `validateSlug` to prevent directory traversal, but does not re-validate in every context (e.g., if called from a new code path in the future).
- **No explicit check** for symlink attacks or race conditions if the adversarial directory is replaced between validation and file creation.

## 4. Logic Contradiction Finder

- No direct contradictions between In Scope/Out of Scope across the three tasks.
- Task dependencies are correctly ordered (P3-008 → P3-009 → P3-010).
- No evidence of incompatible changes to the same file in different tasks.

## 5. Context Blindness Probe

- All referenced files appear to exist in the repo structure.
- No evidence of missing go.mod dependencies (markdown-only changes).
- No violation of AGENTS.md invariants detected.

## 6. Failure Mode Analysis

- If the Fix Type table is omitted or malformed, downstream agents may fail to parse required changes, leading to silent protocol drift.
- If the new report path format is not adopted everywhere, some tools may fail to locate reports.
- If the reviewer agent tool lists are trimmed too aggressively, future review features may be blocked.

## 7. Hallucination Audit

- No external library APIs are cited without a Planning Artifact; all changes are to markdown, agent, and skill files.
- All identifiers referenced are present in the repo or are new table/section names.

---

## Required Changes

| Priority  | Attack | Issue                                                                 | Fix Type          | Concrete Action                                                                                 |
|-----------|--------|-----------------------------------------------------------------------|-------------------|-----------------------------------------------------------------------------------------------|
| BLOCKING  | A3     | Path traversal defense relies solely on validateSlug; no re-check if new code path is added | SPEC_EDIT         | Add explicit comment in adversarial.go: every use of PlanSlug as a directory must call validateSlug, even if code is refactored. |
| ADVISORY  | A2     | No test for non-writable nested report directory                       | TEST_NAME         | Add a test to adversarial_test.go: simulate non-writable directory and assert error is handled. |
| ADVISORY  | A6     | No test for malformed Fix Type table in reviewer output                | TEST_NAME         | Add a test to EsquissePlan.agent.md: simulate missing/malformed Fix Type table and assert error is surfaced. |
| ADVISORY  | A3     | No explicit check for symlink/race condition on adversarial dir        | SPEC_EDIT         | Add a note in adversarial.go: consider atomic directory creation or check for symlinks before writing. |

---

## Verdict
Verdict: CONDITIONAL
